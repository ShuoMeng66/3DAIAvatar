# Session UI-3 完成

## 交付物
- `USE_WEBRTC` / `VITE_USE_WEBRTC` 配置
- `ChatPage` 简单模式：静态图 + audio_url 播放
- `AvatarPlayer` / `VoiceButton` / `SubtitleBar` / `ChatHistory` 紫色 UI
- `ConnectionStatus` 组件（UI-4 复用）

## 验收
- [x] 聊天页紫系大按钮大字号
- [x] VITE_USE_WEBRTC=false 时不连 WebRTC，走 TTS
